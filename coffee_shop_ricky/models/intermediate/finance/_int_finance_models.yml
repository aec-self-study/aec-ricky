version: 2
 
models:
  - name: int_customers_new
    description: Cleaned up data for our customers
    columns:
      - name: customers_id
        description: Primary key of the model
        tests:
          - unique
          - not_null